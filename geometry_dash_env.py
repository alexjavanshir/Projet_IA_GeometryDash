import gym
from gym import spaces
import numpy as np

class GeometryDashEnv(gym.Env):
    metadata = {"render.modes": ["human"]}
"""
    def __init__(self):
        super(GeometryDashEnv, self).__init__()
        # Définir les dimensions de l'observation (position ou vitesse)


    def reset(self):
        #Réinitialise l'environnement à l'état initial


    def step(self, action):
        #Avance d'une étape dans l'environnement
        


    def render(self, mode="human"):
        #Affiche l'environnement (optionnel si déjà géré dans Pygame)
        pass
"""